trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  # Change these to match your actual Azure names
  registryName: 'vueapp.azurecr.io'
  imageName: 'my-vue-app'
  containerAppName: 'my-vue-live-app'
  resourceGroup: 'vue-project-rg'

steps:
# 1. Build the Docker Image and Push it to Azure Registry
- task: Docker@2
  displayName: 'Build and Push to ACR'
  inputs:
    containerRegistry: 'Azure-Cloud-Connection'
    repository: '$(imageName)'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'

# 2. Deploy the new image to Azure Container Apps
- task: AzureCLI@2
  displayName: 'Deploy to Azure Container Apps'
  inputs:
    azureSubscription: 'Azure subscription 1'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      az containerapp update \
        --name $(containerAppName) \
        --resource-group $(resourceGroup) \
        --image $(registryName)/$(imageName):latest
